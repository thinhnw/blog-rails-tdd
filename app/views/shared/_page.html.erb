<article>
  <h2>
    <%= link_to page.title, page_path(slug: page.slug) %>
  </h2>
  <p>
    <%= page.created_at.to_fs %>
  </p>
  <%= content.html_safe %>
  <p><%= tag_links(page.tags).html_safe %></p>
</article>